# Dependências da pipeline (Tech Challenge Fase 2)
# Instalação: pip install -r requirements.txt

pandas
pyarrow                          # motor Parquet do pandas
pandas-gbq                       # leitura do BigQuery para DataFrame
pydata-google-auth               # autorização da conta Google pelo navegador
google-cloud-storage             # operações no bucket do data lake (GCS)
google-cloud-bigquery-storage    # API de download em alta velocidade do BigQuery
                                 # (sem ela, extração de milhões de linhas usa a
                                 # API REST e leva dezenas de minutos)
gcsfs                            # permite ao pandas ler/gravar em gs://
google-cloud-pubsub              # mensageria do streaming (topico, subscription, DLQ)
matplotlib                       # visualizacoes nos notebooks de desenvolvimento
